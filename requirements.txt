# GitPulse v0.1 (Foundation) dependencies.
# The pure-logic core (classification, feature extraction, scoring) needs only
# the Python stdlib. The packages below power the git/GitHub and storage layers.

GitPython>=3.1        # read commit history from a local clone
PyGithub>=2.3         # GitHub REST API (repo metadata; used more in later versions)
radon>=6.0            # cyclomatic complexity for Python files
pymongo>=4.7          # MongoDB driver (primary datastore)
python-dotenv>=1.0    # optional: .env loading (a stdlib fallback is built in)

# --- optional: XGBoost "second opinion" model (Tool 1 ML variant) ---
# Only needed if you train/use the ML model; the weighted scorer works without these.
xgboost>=2.0          # gradient-boosted defect-risk classifier
scikit-learn>=1.3     # cross-repo / held-out-language evaluation, CV
numpy>=1.24           # feature matrices

# --- optional: LLM provider layer (v0.2) ---
# Install only the SDK for the provider you choose (LLM_PROVIDER). All LLM
# features degrade gracefully when these aren't installed / no key is set.
openai>=1.0           # LLM_PROVIDER = local (LM Studio) | openai | gemini
anthropic>=0.40       # LLM_PROVIDER = claude

# --- optional: PR Review similarity engine (v0.3) ---
# Without these, Tool 3 uses a built-in lightweight (TF-IDF) similarity index.
# Installing them upgrades to real semantic embeddings + a vector DB.
sentence-transformers>=2.2   # diff embeddings (pulls in PyTorch — large download)
chromadb>=0.5                # local vector store

# --- FastAPI backend (v0.4) ---
# Only needed to run the web API (`python -m uvicorn api.main:app`); the CLI
# and all four tools work without them. httpx is only for tests/test_api.py.
fastapi>=0.110         # web API + PR webhook
uvicorn[standard]>=0.29  # ASGI server
httpx>=0.27            # FastAPI TestClient dependency (tests only)

# --- arrive in later milestones ---
# motor                   (async Mongo read path, if the sync driver is outgrown)
